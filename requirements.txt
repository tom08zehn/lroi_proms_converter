# LROI PROMs Converter - Runtime Dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Python 3.11+ is required (uses stdlib tomllib).

openpyxl>=3.1.0
lxml>=5.0.0

# For building Windows .exe, also install:
# pip install pyinstaller
