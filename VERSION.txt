LROI PROMs Converter
Version: v1.4.6
Release Date: 2026-02-27

Fixes in v1.4.6:
- Fixed GUI "Use default" checkbox not ticked on startup
- Fixed CLI arguments being pre-resolved and passed to GUI
- Updated README.md configuration section (complete rewrite)
- Updated INSTALL.md with correct paths and file naming

GUI Checkbox Fix:
- Problem: "Use default" checkbox unchecked when launching GUI without CLI args
- Cause: output_path resolved before GUI launch, passed to prepopulate even when None
- Fix: Only pass CLI args to prepopulate if explicitly provided by user
- Result: ✓ Checkbox properly ticked, templates shown correctly

Documentation Updates:
- README.md: Complete configuration section rewrite based on demo.config.toml
- Explains all aspects: required vs optional settings, value conversions, regex matching
- Clear examples showing how PROM detection and lookups work
- INSTALL.md: Updated paths (demo/demo.config.toml) and file naming (v1.4.5+ format)

Verified Working:
- ✓ GUI opens with "Use default" checked for all file fields
- ✓ CLI --output switch properly overrides in GUI  
- ✓ Timestamps consistent across log/xlsx_log/output
- ✓ Documentation accurate and comprehensive

See CHANGELOG.md for full version history.
